# kmer_freq-cosine_similarity
Implemented a function that takes a DNA sequence string and returns a dictionary of all k-mer frequencies for a given k, and a function that takes two such k-mer frequency vectors and computes their cosine similarity using NumPy.
